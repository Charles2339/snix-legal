# Cloudflare Pages serves clean URLs natively via "Pretty URLs" setting —
# no explicit rewrite rules needed here. Explicit / → /index.html rewrites
# caused ERR_TOO_MANY_REDIRECTS when Pages' built-in pretty-URL logic
# also tried to redirect /index.html → / (infinite loop).
#
# Leave this file intentionally minimal. If you ever need true 301/302
# redirects (e.g. old URL → new URL), add them here.
